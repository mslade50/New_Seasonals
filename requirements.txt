pandas
exchange-calendars>=4,<5
pyarrow
numpy<2  # sklearn 1.3.0 (pinned below) imports ComplexWarning from numpy.core.numeric, removed in numpy 2.0; keep on 1.x until the ml/ model is retrained on a newer stack
yfinance
streamlit>=1.23.0
plotly
ta
requests
urllib3>=1.26,<3  # EP hardened fetcher imports urllib3 TLS connection primitives directly
openpyxl
yfscreen
yahooquery
scipy
matplotlib
mplfinance
scikit-learn==1.3.0  # pinned: ml/ meta-model pickle (HGB) must match train-time sklearn; unpin only when retraining + re-uploading the R2 artifact
gspread
requests-cache
pytz
kaleido
fpdf2
boto3
python-dotenv
