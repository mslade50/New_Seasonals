-r ../requirements.txt

# Explicit runtime dependencies used by the scheduler/session gates.  They
# may arrive transitively today, but the unattended runtime must not depend
# on that remaining true.
exchange-calendars
google-auth
pytest

# Official economic release PDF parser.
-r ../requirements-official-data.txt
