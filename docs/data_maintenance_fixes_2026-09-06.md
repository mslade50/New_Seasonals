# Additional data-review source fixes — 2026-09-06

These source-only changes close the review's MTM date/calendar, reference-ticker collision and stale research-page cache findings. They are separate from the controlled Seasonal impact comparison. No thresholds, risk settings, canonical data, services or published outputs were changed.

- `pages/strat_backtester.py:get_daily_mtm_series` derives its default end from supplied trade/price data instead of wall-clock today. The default grid uses the shared NYSE calendar; callers can provide `end_date` and explicit `session_dates` for another market. An early as-of view does not book a later realized exit, and a late start returns daily changes from the retained prior marks. `calculate_mark_to_market_curve` forwards the explicit bounds/calendar.
- Reference rank columns are keyed by **reference ticker and horizon**. Each strategy selects its own columns at filter evaluation without mutating the shared frame. Required horizons are unioned across strategies using the same reference ticker. This fixes both different tickers overwriting one horizon and later strategies losing an additional horizon. The feature remains configuration-driven; no current strategy is enabled or retuned.
- The live file/price/metric loaders in `equity_seasonals`, `macro_seasonality`, `correlation_heatmaps` and `heatmaps` expire after one hour. The heatmap's cached SPY context and calculation that excludes the Seasonal map from its cache key are bounded too. Expiry is checked on a subsequent call; it does not create a background refresh process.

Validation: **52 focused checks passed**, including four new behavioral regressions. Those same four regressions all fail against the untouched audit baseline: wall-clock/holiday expansion, unavailable as-of/calendar interfaces, and reference masks `[True, True, False]` instead of the intended `[False, True, True]`. Source compilation and whitespace checks pass. Evidence: `artifacts/data-fixes/maintenance-tests.log` and `maintenance-baseline.log`.

The final Seasonal replay loaded its engine before these source edits. It uses neither MTM-series aggregation nor any enabled reference-ticker filter, so the recorded candidate/fill/sizing comparison remains isolated. The separate research repair stream owns the causal delayed-limit simulator, three portable research-script paths and reviewed finite-cost Databento patch. Canonical Seasonal/ledger regeneration and promotion remain a separate financial-history action.
