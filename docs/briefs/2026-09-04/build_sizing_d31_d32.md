# Brief: build_sizing_d31_d32 (retire the WCDS seasonal size tiers; add the base-bps tilt at import)

Date 2026-09-04. Plan: `docs/plan_2026-09-04.md` (section 0, D3 items 1-2, section 6). Type: BUILD (money path: a VERIFY agent follows; you never verify your own work beyond the tests below).

## Decision and why
D3.1: Weak Close Decent Sznls carries a legacy seasonal size multiplier (1.5x at seasonal rank >= 65, 0.66x at 33-50, 1.0x elsewhere) in both the scan and the engine. The 2026-09-02 due diligence measured it as inverted against the edge (the 1.5x bucket earns +0.23R, the 0.66x bucket +0.71R; sleeve Sharpe 0.62 -> 0.70 with it removed). It is retired: WCDS sizes at 1.0x at every seasonal rank.
D3.2: A per-strategy base-bps tilt, applied once at import time in `strategy_config.py` after the GRM block, to `execution['risk_bps']` only. Table: 52wh Breakout 0.70, Weak Close Decent Sznls 0.75, Sector BO 0.87, ATR Extended Gap Up 1.10, 3x ETF Overbot Fade 1.27, SPY QQQ MonFri Reversion 1.30; every other strategy 1.0 (list them explicitly in the dict so the pin is total). Evidence: the keep-adjusted allocation study and its engine replay (Sharpe 2.17 -> 2.26, maxDD -15.2 -> -13.1, worst-21d -10.1 -> -8.0, PnL-neutral). Why import time and not `risk_multipliers`: the engine's `risk_multipliers` path scales the per-strategy daily cap with the multiplier, which would give MonFri a 325 bps engine cap against a fixed 250 live. Why not the earnings override: the override replaces the base and is an appetite haircut frozen in effective bps. Why not `path1_bps`/`path2_bps`: OVS is 1.0.

## Files you own
`strategy_config.py`, `daily_scan.py`, `pages/strat_backtester.py`, `daily_portfolio_report.py` (only if the overflow-override site there needs the tilt folded in), new tests `tests/test_wcds_size_tiers.py` and `tests/test_base_bps_tilt.py`, and any existing test that pins the WCDS tiers or the exact nominal bps of a tilted strategy (list each one you touch and why). Nothing else.

## Hard rules
Section 0 of the plan. No ledger rebuild, no upload, no Sheets. The tree may be dirty in unrelated files (pitch_lab.py, fundamental/, requirements.txt, data/): leave them alone. Do not touch `order_staging.py` (it takes staged sizes as is). Do not change any number outside the two decisions above.

## Intent
1. D3.1: remove the WCDS seasonal tier branch from the scan's sizing step (search for the `Weak Close Decent Sznls` branch in `run_daily_scan`'s sizing, and the sizing-note helper that prints it) and from `process_signals_fast`'s corresponding step (inside the overlay-enabled block). After the change no code path multiplies WCDS risk by seasonal rank. The scan's Sizing note must no longer mention a seasonal tier for WCDS.
2. D3.2: `STRATEGY_BASE_TILT: dict[str, float]` in `strategy_config.py`, total over `STRATEGY_BOOK` names (a test asserts every strategy has an entry). Apply in the same import-time block that applies `GLOBAL_RISK_MULTIPLIER`, after it, to `execution['risk_bps']` only. `OVERFLOW_RISK_OVERRIDES` must carry the same tilt for any strategy in it (fold the tilt into the override values at import, next to where GRM scales them), so the three overflow-override consumers (scan, engine, portfolio report) stay consistent without edits; state in the report whether any tilted strategy is in that dict today. The scan's per-signal Sizing note gains a `tilt 0.70x` style fragment whenever the tilt is not 1.0 (so the scan email shows it); the engine needs no note. Do not add a `risk_multipliers` argument path.
3. Tests. `tests/test_wcds_size_tiers.py`: on a WCDS fixture at seasonal ranks 70, 55, 40, 20 the engine's `Size_Mult` is 1.0 and the scan's sizing note carries no tier text (use the same fixture pattern the existing sizing tests use, e.g. `tests/test_same_day_derate.py`, `tests/test_frag_risk_bands.py`). `tests/test_base_bps_tilt.py`: the table is exactly the decision's table; the dict is total over the book; effective `risk_bps` for each tilted strategy equals nominal x GRM x tilt; the earnings override `risk_bps` is untouched; `path1_bps`/`path2_bps` untouched; the overflow override for a tilted strategy (if any) carries the tilt; `scripts/build_trade_ledger.py` passes no `risk_multipliers`; import is idempotent (importing twice does not tilt twice; check how the GRM block guards this and mirror it).
4. Any existing test that pinned an exact effective bps for a tilted strategy is updated to the new value with a one-line comment citing D3.2; list them.

## Recon first
Write `artifacts/build_2026-09-04/sizing_d31_d32/00_plan.md`: every site you will edit with line ranges you found, the GRM-block idempotence mechanism, and which existing tests pin affected values. Then build.

## Verification
- `python -m pytest -q tests/ -p no:cacheprovider` must be 0 failed (the tests-hygiene builder is running in parallel on three other test files; if their marks are not in yet, the pre-existing 7 failures there are not yours; list them).
- A read-only replay on a slice: run `process_signals_fast` through `scripts/build_trade_ledger.py`'s book builder on 2018-01-01..2019-12-31 (flat basis, no upload, output to your artifacts folder only; read `scratch/ultracode_sizing_2026-09-02/dd_engine/engine_partial_replay.py` for how the DD invoked the engine with all caps) once with the tree as you found it (stash nothing; instead reconstruct the "before" by temporarily setting all tilts to 1.0 and the WCDS branch on via a flag you add for the test only if that is cleaner than a second checkout; state what you did) and once after. Produce `artifacts/build_2026-09-04/sizing_d31_d32/checks.json`: `{"tests_failed": int, "tilt_table_total": bool, "wcds_rows_changed": int, "wcds_rows_total": int, "non_wcds_untilted_rows_changed": int, "tilted_strategies_size_ratio": {"52wh Breakout": float, ...}, "override_rows_changed": int, "files_touched": [...]}`. Expected: `non_wcds_untilted_rows_changed == 0`, `override_rows_changed == 0`, each tilted strategy's median Size_Mult ratio equals its tilt within 1e-6 outside cap-bound days.
- No screenshots.

## Report
Section 6 format. Handoff: the `git add` list; any existing test whose pinned value you changed; anything the verify agent should attack first.
