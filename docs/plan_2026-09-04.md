# Fortnight plan: 2026-09-04 to 2026-09-18

Authority for this fortnight is this file plus `docs/running_list.md`. A new
session or agent starts by reading both. The "mind" (the orchestrating Claude
session) makes every design, code and research decision here; agents build
only what a brief in `docs/briefs/2026-09-04/` tells them to build. McKinley
owns the calls marked OWNER.

Evidence behind the decisions: the 2026-09-03 deep dive (four completed
lenses: book edge, ops health, sizing decision packet, agent products; the
other lenses were cut short and are re-run as recon here), the sizing plan
and its due diligence at repo root (`sizing_optimal_plan_2026-09-02.html`,
`sizing_due_diligence_2026-09-02.html`), and CLAUDE.md.

## 0. Hard rules for every agent

1. Read CLAUDE.md's invariants before touching anything: dividend-adjustment
   basis, aligned-sites contracts, append-only fragility parquet, the
   no-notional-caps rule, engine/live parity.
2. Touch only the files the brief says you own. Never rename, move or delete
   a file. Never commit, stash, checkout or push. Never write to Google
   Sheets, R2, Cloudflare, IBKR or Task Scheduler. Never run `daily_scan.py`,
   `order_staging.py`, any `*_moo.py`, or any workflow. No `pip install`.
3. Never invent content or numbers. A number in a report is either measured
   by a script you wrote (path cited) or quoted from a file (path cited).
4. Do your own recon. Read the code, write an exact-edit plan to a scratch
   file under `artifacts/` or the session scratchpad, then build. The brief
   is decision-level; the line numbers are yours to find.
5. Money-path rule: any change to `daily_scan.py`, `pages/strat_backtester.py`,
   sizing fields in `strategy_config.py`, `filters.py`, or anything under
   `OneDrive\trading_ibkr` gets an independent VERIFY agent (separate brief)
   that tries to break it against the engine harness and the tests before
   the mind reviews. The builder never verifies its own work.
6. One writer per file set at a time. If the brief says the tree may be
   dirty, build on top of what is there.
7. Report in the fixed format in section 6. Screenshots are named in the
   brief; the checks JSON must be produced by a script, not typed.

## 1. Decisions

**D1. Freeze.** Exactly one more runtime cutover in the fortnight: the one
that puts `harvest_fills` into the pinned runtime. It happens after the
09-03 incident write-up exists in `docs/`, outside 04:00-09:35 ET, on a day
with no other change. No new products, no new agent surfaces. Everything
that ships is a fix, a measurement, or an item in D3.
Why: two failed recoveries in six days, the health check dead since
2026-08-14, five cutovers in a week. Nothing else matters if a scan does not
stage.

**D2. Finish the deep dive with four recon lenses**, not eleven: the OneDrive
execution code, engine/scan parity (including the OLV pivot-aware entries and
the recent PR set), the data adjustment window behind the SPG/UNH splits, and
the worktree triage. Docs drift, the site, regime gaps are deferred and
listed as such in the running list. Recon agents write nothing but scratch.

**D3. Sizing ship list, in this order, each behind the dd_engine harness at
GRM 1.5 before the multiplier moves:**
1. Retire the WCDS legacy seasonal multiplier (inverted against its edge).
2. Base-bps tilt at import time: 52wh 0.70, WCDS 0.75, Sector BO 0.87,
   ATR Ext 1.10, 3x ETF Fade 1.27, MonFri 1.30; every other strategy 1.0
   (OLV, OVS, LT Trend, Monday Dip, St OS Sznl, IOB, the pilots). IOB stays
   1.0 by the mind's call: its sign-flip rate was borderline and the packet
   said skipping the cut costs nothing. Never through `risk_multipliers`
   (that path scales the engine's per-strategy cap); never applied to the
   earnings override.
3. Clone clamps: IOB SPY+QQQ same-day 0.5x (IOB only; MonFri must not carry
   it) plus the five new cross-strategy overlap pairs at 20 bps nominal.
4. WCDS / LT Trend adds rule, re-keyed to "same-day staged count >= 2 OR a
   prior open leg", 0.8x solo / 1.2x otherwise, generic `open_leg_mults`,
   identical in scan and engine. Re-scored once on the ledger before merge.
5. OVS bottom-extremity 0.7x (mean of rank_2/5/10/21 < 94), with a midterm
   exemption (1.0 when year % 4 == 2, where the 0.75 cycle mult already
   applies). Engine parity for 2027; no live effect in 2026.
6. GRM 1.5 -> 1.875 with overflow-long exclusion (OLV 20, LT Trend 24,
   St OS Sznl 32, 52wh 28 nominal) LAST, after items 1-5 pass the harness.
   See D4.
Dropped for the fortnight, by name: OLV depth ladder, OLV composite clip,
OVS P2 cap raise (engine no-op), the flow rules, the OLV 1.17 tilt, the OLV
pullback tilt (walk-forward gate is 6 of 15 years, not the 11 of 15 the
plan claimed), any 52wh depth rule.

**D4. GRM (OWNER call, 2026-09-04).** McKinley: treat the Risk Navigator
what-if as accounted for; it is not a gate. So item D3.6 ships in the
fortnight behind the harness only, and the order_staging margin guard is
deferred out of the fortnight. The mind's reservation is recorded once and
not re-argued: on the stylised rates the 2013-11-04 book sits at 95% of live
NLV at 1.25x. The `book_snapshot.py` margin tags (display only) still ship
so the number is visible.

**D5. Pitch approvals auto-stage (OWNER approved 2026-09-04).** The designed
path already exists: `pitch_moo.py` in OneDrive places `Pitch`-tab rows
marked `Y` at 9:05 (auction) and 9:32 (open-anchored limits), gated by
`pitch_moo_enabled.flag`, tasks not registered. Decision: arm that path, do
not reroute pitch rows through `order_staging` (different vocabulary:
MOO/MOC auction legs, Wilder ATR, its own sanity bounds). Steps: a VERIFY
agent audits `pitch_moo.py` against the 2026-08-21 MOC-encoding lesson and
the OneDrive tests; a dry-run against a fixture tab; then McKinley registers
the two tasks and the flag himself via `!` (the permission classifier
blocks sessions from doing it). Sizing stays the grammar's ATR-risk bounds
(60 bps/idea, 150 bps/day, $15k basket). Approval policy is McKinley's: Y
means placed.

**D6. Product cadence.** Pitch: event-triggered (runs only when the B1
surface map holds an anchor not already closed in the negative registry, or
a watchlist entry arms), watchlist entries expire at 20 td, registry indexed
by cell. Posts: paused until the X handle exists. EP shadow heartbeat:
paused until ten clean unattended mornings after its two structural fixes.
Discretionary Focus: 20-session kill rule written down. Fundamental sleeve:
weekly, notify on state change only. Radar: trail flag stays off, no hand
staging. Event sleeve: untouched; eyeball T2 on Sep 10 and T3 on Sep 18.
Every `claude -p` runner logs `total_cost_usd`.

**D7. OLV.** Hold at 1.0x base, no up-lever of any kind this fortnight.
Reconcile the -0.08R live-vs-ledger gap leg by leg from
`data/live_fills.parquet` (it is discretion, not fills), and reconcile the 28
overflow OLV positions since June with no live counterpart. The EOD book cap
stays disabled unless McKinley re-arms it; it becomes a precondition the day
any OLV up-lever is proposed again.

**D8. P/C-fear zeroing post-ship review.** Aug-2026 is a new hi-dial
episode. The decision rule is written by the mind into the study brief
BEFORE the study runs, after re-reading
`scratch/ultracode_research/family_pc_fear_band_prereg_2026-08-05.md`. The
candidate outcomes are closed: keep 0.0x, or move the fear-OFF hi-dial cell
to 0.5x. No other multiplier is on the table.

**D9. OVS liquid-vs-overflow prereg.** Liquid OVS avgR +0.50 through 2023,
-0.03 on 66 positions since 2024 (Welch t 3.3); overflow fine. A
pre-registered study (decision rule first, 2020-2023 held out, clustered by
signal day) runs before any OVS-specific change beyond D3.5.

**D10. Hedge.** Rewrite the beta-hedge prereg as a dial-gated directional
overlay sized on holdings beta (the hedge panel already computes it), with
the exposure-leg conventions, and put ONE MES round trip through the futures
rail, which has never carried a live order. Prereg and rail test only; no
live hedge this fortnight. McKinley's manual short is the paper track and
gets logged.

**D11. Ops fixes (from the ops report's ranked findings).** One health task,
delete the 35 disabled task generations, Task Scheduler operational log on,
runtime logs to a path that survives cutovers, a second S4U premarket task at
05:30 that re-runs the pipeline idempotently, resolve the 09-02
execution_report receipt, fix the CI red (U+2713 in a scratch markdown), and
`ledger_git_sha` populated from `GITHUB_SHA`. Cutover cadence rule written
into `docs/local_automation_task_scheduler.md`.

## 2. Schedule

| Days | Work | Owner |
|---|---|---|
| Sep 4 (Fri) | Plan files committed. Recon wave (D2). Confirm the 04:10 v8 run and the 07:30 health probe worked. Fix CI red. Resolve the 09-02 receipt. | mind + recon agents; McKinley confirms morning |
| Sep 5-8 | D11 ops fixes as build+verify pairs. Incident write-up for 09-03. The one cutover (harvest_fills). D8 rule written; D8 and D9 studies run. | agents; mind writes rules |
| Sep 9-10 | D3.1 and D3.2 (WCDS multiplier, tilt) build + verify + harness. D5 pitch_moo verify agent. Sep 10: eyeball the T2 Event row (self-skips if SPY rank21 lag-1 >= 50). | agents; McKinley eyeballs |
| Sep 11-14 | D3.3, D3.4 build + verify + harness. D6 cadence changes. D7 OLV reconciliation. | agents |
| Sep 15-17 | D3.5, then D3.6 behind the harness; ledger deploy with vintage diff reviewed. D10 prereg + MES round trip. McKinley registers pitch_moo tasks. | agents; McKinley for the MES trade and the task registration |
| Sep 18 (Fri) | Eyeball the T3 Event row. Dial-sleeve gate-2 status note. Fortnight close-out: running list reconciled, CLAUDE.md "Sizing Hierarchy Revised" section. | mind |

## 3. Calendar and gates inside the window

- Sep 10: FOMC. Event sleeve T2 (midterm short SPY 10%, only if SPY 21d rank lag-1 < 50).
- Sep 18: quad witching. Event sleeve T3 (short IWM 15%, skipped if IWM z10 lag-1 < -1). MES/ES September expiry: the D10 round trip must use the December contract or roll.
- Mid-September: dial-gated SPY sleeve paper track gate 2 (FLAT since 07-17, 0 transitions; nothing to sign off unless the dial releases).
- The Aug-2026 hi-dial episode is still open (10d-MA 63d 87.9 on 09-02). Several preregs become runnable when it releases below 45-50.

## 4. Questions for McKinley (OWNER)

1. D5: confirm Y on every leg means placed at the grammar's size, and that a partly-approved multi-leg idea refusing the whole basket is the behaviour you want (it is the current code).
2. D7: re-arm the EOD OLV book cap now, or leave it for the day an OLV up-lever is proposed?
3. D6: approve the pitch cadence change (event-triggered) or keep daily.

## 5. Brief skeleton (every agent brief uses this)

```
# Brief: <name>            Date, mind's session id
## Decision and why        (two to five sentences; the "what" is fixed)
## Files you own           (exact paths; everything else is read-only)
## Hard rules              (section 0 of docs/plan_2026-09-04.md, plus any brief-specific rule)
## Intent                  (enough that two agents would build the same thing)
## Recon first             (write your exact-edit plan to <scratch path> before editing)
## Verification            (what to run, what to measure, named screenshots, checks JSON path + keys)
## Report                  (section 6 format)
```

## 6. Fixed report format (every agent's final message)

```
REPORT: <brief name>
1. What changed        - file paths, one line each; "nothing" for recon
2. What ran            - commands, exit codes
3. Checks              - the checks JSON verbatim (path)
4. Screenshots         - names + paths, or "none required"
5. Findings            - ranked, each with evidence path and a number
6. Could not verify    - explicit list
7. Handoff             - what the next agent or the mind must decide
```
